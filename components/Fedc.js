import react from 'react'
import styled from 'styled-components'

const Fedc = ({ className }) => (
	<img className={className} src="/static/fedc_w.svg" />
)

export default styled(Fedc)`
	display: block;
`
